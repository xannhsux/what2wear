import Foundation

/// Copy this file to Secrets.swift and fill in your real keys.
/// Secrets.swift is gitignored and will never be committed.
enum Secrets {
    static let replicateAPIKey = "YOUR_REPLICATE_API_KEY_HERE"
}
